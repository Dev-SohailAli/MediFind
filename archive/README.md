# Archived prototypes

This directory contains historical experiments that are not part of MediFind's
active product or workspace. The only entry currently archived is the former
Expo/React Native prototype. Do not build, extend, install, or use it as a task
starting point. The active product is the responsive web/PWA in `apps/web` and
the optional Cloudflare Worker boundary in `apps/worker`.
